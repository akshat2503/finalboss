{
  "name": "finalboss-kind",
  "version": "1.0.0",
  "description": "Ephemeral K8s environments using Kind",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
